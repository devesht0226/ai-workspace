# AI Workspace Web

Next.js frontend for the AI Workspace platform.

## Local development

```bash
cd apps/web
npm install
npm run dev
```

App: http://localhost:3000

Set `NEXT_PUBLIC_API_URL` (default `http://localhost:8000`) to point at the API.
